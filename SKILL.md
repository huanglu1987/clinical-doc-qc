---
name: clinical-doc-qc
description: 全面核查临床试验申报资料（方案、ICF、IB、CSR、IND/NDA 模块等）的一致性、逻辑性、语言自然性与格式规范。支持单文件内部核查与多文件交叉核查，按 CDE（中文）或 FDA（英文）两套规则集判定格式合规性。当用户要求"审核/核查/QC/检查"临床试验方案或申报资料，或提到文档前后矛盾、术语不一致、格式是否符合 CDE/FDA 要求时使用。
---

# 临床试验资料核查（clinical-doc-qc）

## 一、这个 Skill 的核心前提：两层分离

**必须先理解这一点，否则会给出编造的结论。**

你（AI）读到的文档是纯文本，**不携带字体、字号、行距、页边距信息**。
仅凭"读一遍"判断排版是否合规，只会产生看似专业实则虚构的判断。

因此本 Skill 把核查严格分成两层，**两层都必须做，且不能互相替代**：

| 层 | 查什么 | 由谁执行 | 铁律 |
|---|---|---|---|
| **格式层 + 结构层** | 字体、字号、行距、颜色、页边距、纸张；表图编号连续性、交叉引用悬空、缩写定义冲突 | `scripts/docqc.py` | **必须实际运行工具**。禁止凭阅读给出任何字体/字号/页边距结论 |
| **文本层** | 内容前后矛盾、逻辑自洽性、语言自然性 | 你，按第三节方法论 | 禁止退化成关键词比对 |

> 违反上述任一条铁律得出的发现，**不予采信**。

---

## 二、格式层：先跑工具，再解读

### 第一步：预检

```bash
python3 ~/.codex/skills/clinical-doc-qc/scripts/docqc.py --help
```

失败则说明依赖缺失，执行：

```bash
pip3 install python-docx pypdf pymupdf
```

### 第二步：按语种选规则集

| 资料语种 / 递交去向 | 规则集 | 命令 |
|---|---|---|
| 中文资料，递交 CDE | `cde` | `docqc.py check <文件> --profile cde` |
| 英文资料，递交 FDA | `fda` | `docqc.py check <文件> --profile fda` |
| 中美双报同一品种 | 两套都跑 | 分别跑，两边规则不同（纸张 A4 vs Letter、中文字体要求只在 CDE 侧） |

多份文件一起传，会自动附带跨文件一致性检查：

```bash
python3 .../docqc.py check 方案.docx ICF.docx IB.docx --profile cde
```

只看跨文件差异：

```bash
python3 .../docqc.py compare 方案.docx ICF.docx IB.docx
```

### 目录页码：只有 PDF 能判，docx 不能

**这是最容易出错的一项，务必按下表行事：**

| 格式 | 目录标题比对 | 目录页码比对 |
|---|---|---|
| `.docx` | ✅ 工具已查 | ❌ **不得判定**——页码是域的缓存值，Word 打开/转 PDF 时会重算 |
| `.pdf` | ✅ 工具已查 | ✅ 工具已真正比对（按众数偏移，已排除封面导致的整体偏移） |

对 `.docx` 报"目录页码错误"，会重犯方法论第 8 条记录的真实反例
（曾据缓存值判定"转 PDF 会显示错误页码"，实测后完全不成立）。
遇到 docx，正确说法是：**提示用户更新域后导出 PDF 再核对页码**，而不是给出页码结论。

### 第三步：解读工具输出

- `✗ 错误`：违反明文规定，**必须改**
- `⚠ 警告`：合规但异常（如边距远大于规定值），通常是格式不统一的痕迹，需人工判断
- 工具报的是**事实**（"正文主字体为微软雅黑"），你负责把它转成**给人看的整改说明**

**不要把工具输出原样粘贴给用户**。要合并进最终的问题清单，并补上"改哪里、怎么改"。

**`.doc` 老格式工具读不了**，遇到时告诉用户先在 Word 里另存为 `.docx`。

---

## 三、文本层：按这套方法核查，不要用字符串比对

方法论全文见 [`references/methodology.md`](references/methodology.md)，**开始文本层核查前必须完整读一遍**。
以下是不可省略的强制动作：

### 1. 先建「概念清单」，不是「数据点清单」

数据点是名词（例数、剂量、日期）；概念是**动词与规则**（怎么做、什么时候做、谁决定、多长时间、什么情况例外）。
**互斥矛盾几乎全藏在动词里。**

先列出 8–15 个关键操作概念，把全文相关句子**全部摘出来**，两两比对是否可同时成立。
跳过这一步直接比对数值，等于只做了危害最低的那一类检查。

#### ⚠ 用户点名的"重点项"是叠加，不是替代（强制）

当用户在需求里列出了若干重点核查项（如"重点看入排标准、剂量方案、访视安排"）：

1. **必须仍然独立通读全文、自行枚举概念清单**，覆盖范围由文档内容决定，
   **不得**把用户列的那几项当成核查范围的全部。
2. 用户点名的项**并入**清单并优先处理；用户没点名但你枚举出来的项，**照查不误**。
3. 若用户明确要求"只查这几项"（而不是"重点查"），则必须在报告开头
   **显式声明本次未覆盖的范围**，不能默默缩小。

**理由**：用户列举时通常是凭印象举例，而不是穷举——他知道的风险点，
恰恰是已经想到、因而不太容易出错的部分；真正的问题往往在没被想到的地方。
把用户的举例当成范围上限，等于系统性地避开最可能出问题的区域。

这与本方法论第 9 条同源：**沉默不等于一致**——没查过的部分，
不能因为报告里没提就被读成"查过且没问题"。

### 2. 三类不一致分开查，重点在第二类

| 类型 | 表现 | 危害 | 谁能查 |
|---|---|---|---|
| 表层不一致 | 同一数值/名称写法不同 | 低 | 工具 / 关键词 |
| **语义不一致** | **不同措辞表达出互斥要求** | **最高** | **只有你能查** |
| 结构不一致 | 编号、引用、合计、时态 | 中高 | 工具已覆盖大部分 |

### 3. 数值必须追依赖链，不是孤立核对

通用链条模板：
`单位用量 → 频次 → 单次小计 → 累计总量 → 据此推导的阈值 → 引用该阈值的入排标准 → 对外披露文件（ICF/摘要）`

链上任一环节改动，其余全部重算。**表格分项之和必须与所载合计逐个验算。**

### 4. 高危措辞专项复核

- 凡「全部 / 唯一 / 完整 / 汇总了所有」等**封闭性措辞**，逐条穷举验证是否真的完整
- 凡**条件性 / 例外性条款**，代入具体场景实际走一遍，确认物理上可执行
  - 必走场景：提前退出、提前终止、中断后恢复、周末与节假日、极值人群（最低体重/最高年龄）、某中心不具备某条件
- 凡**法定措辞**回查是否确实适用于本研究，不要假设模板照抄就对

### 5. 「未命中」不是结论，「沉默」不等于一致

- 判定为"缺失/未提及"的项，一律标记为**待人工复核**，不得直接写成结论（实测约三分之一是假阴性）
- 每项结论必须三选一：**一致 / 不一致 / 不适用**——先判断该文件是否*本应*涉及这一项

### 6. 错别字、用词错误与语言自然性

判据见 [`references/language-check.md`](references/language-check.md)，分三档处理：

- **错别字**（同音字、形近字、多字漏字、单位大小写 mL/mg/μg）——**必报**，有明确对错
- **用词错误**——**必报**，指容易混用但含义不同的词，在临床试验语境下会造成实质歧义：
  不良事件/不良反应、发生率/发病率、疗效/有效性、剂量/剂型/规格、
  入选/入组/筛选、失访/脱落/退出
- **语言自然性**（欧化句式、Chinglish、时态、情态动词）——按判据报，
  不要给"建议润色"式的无效意见

同一错误重复出现时**合并成一条并注明次数**，不要逐处刷屏，否则会淹没重要问题。

---

## 三之二、两种核查模式

用户的需求不同，投入也应不同。**先判断是哪一种，不要一律按最重的做：**

| 模式 | 何时用 | 做什么 | 不做什么 |
|---|---|---|---|
| **深度核查**（默认） | 正式提交前、多份资料交叉核查 | 第二节 + 第三节全部 | —— |
| **格式 + 文字校对** | 定稿前快速过一遍；用户明确说"只看格式和文字""校对一下" | 格式层工具（含定稿前校对项）+ 错别字 + 用词错误 + 目录与正文比对 + 编号/交叉引用/缩写 | 不建概念清单、不追数值依赖链、不做语义矛盾比对 |

**校对模式下，工具报出的这三项必须置顶呈现**，因为带着它们递交出去属于事故：
残留修订痕迹、残留批注、域代码错误（"错误！未找到引用源"）。
不要把它们混在一堆格式警告里，用户很可能一眼扫过去就漏了。

**选了校对模式时，必须在报告开头声明**："本次为格式+文字校对，未做深度逻辑与一致性核查"，
让用户清楚知道哪些没查——这与第三节第 1 条的覆盖范围声明要求一致。

---

## 四、双通道核查（正式提交材料必做）

两个独立执行实例用**同一份提示词**核查**同一批文件**，查出来的问题几乎不重叠。

**实测**：两份独立报告合并去重后共 30 条发现，其中**两边都查出来的只有 1 条**，
其余 29 条都是只有一方发现、另一方完全漏掉的。
即：**单独跑一轮会漏掉另一轮能找到的绝大部分问题**——不是那些发现不成立，
而是"只跑一轮"本身不可靠。原因是两边方法偏好不同、盲区也就不同。

**这一步 Skill 无法自动完成，必须提醒用户手动执行：**

1. 用 [`templates/review-prompt.md`](templates/review-prompt.md) 生成本项目的核查提示词
2. 在**两个互不共享历史的新会话**里各跑一遍（不是同一个会话跑两次）
3. 人工合并两份结果，**逐条回原文验证**对方的发现（实测会有误报）

正式提交材料时若用户跳过这一步，要明确告知风险，不要默认单轮结果已经够用。

---

## 五、输出格式

### 报告开头必须先声明覆盖范围

在问题清单之前，先给出**本次实际建立的完整概念清单**（8–15 项），并标注每项的核查结论：

| 概念 | 来源 | 结论 |
|---|---|---|
| 提前退出流程 | 自行枚举 | 不一致（见 #3） |
| 剂量调整规则 | 用户点名 | 一致 |
| 揭盲条件 | 自行枚举 | 不适用（本方案为开放标签） |

**这张表的作用是让"查了什么、没查什么"可见**，避免读者把"报告里没提"误读成"查过且没问题"。
若有任何范围限制（用户要求只查某几项、某文件无法解析、某部分需人工），
必须在这里显式列出，**不得静默省略**。

### 问题清单

用表格，**每条都必须可被二次审阅者低成本推翻**：

| 序号 | 触发规则 | 严重度 | 位置 | 原文摘录 A | 冲突摘录 B | 结论 | 说明 |
|---|---|---|---|---|---|---|---|

- **触发规则**：概念清单 / 语义比对 / 数值链 / 封闭措辞 / 条件场景 / 格式层工具 / 结构层工具
- **严重度**：高/中/低，并注明依据——**科学有效性 / 受试者安全 / 法规合规**
- **数值类发现必须给算式**：算式 + 计算结果 + 文档所载值 + 是否相符
- **语义矛盾必须给两处原文**：本处摘录 + 与之冲突的另一处摘录
- **不适用清单单列**，与"一致清单"分开

---

## 六、提交前自检（逐条确认才能出报告）

- [ ] 格式层：**是否真的运行了 `docqc.py`**，而不是靠阅读推断字体字号
- [ ] 文本层：是否先建了概念清单，而不是直接做字符串比对
- [ ] **概念清单是否由自己通读全文枚举得出**，而不是照抄用户点名的那几项
- [ ] 报告开头是否列出了完整概念清单与覆盖范围声明
- [ ] 语义矛盾是否按概念两两比对过
- [ ] 所有"未命中"是否都标了待复核，没有直接下结论
- [ ] 所有结论是否三选一，没有把沉默计成一致
- [ ] 涉及计算的数字是否按依赖链逐环核对，分项之和与合计是否验算
- [ ] 封闭性措辞是否穷举验证，条件性条款是否代入场景走过
- [ ] 每条发现是否标了触发规则、给了两处摘录/算式
- [ ] 是否提醒用户做双通道核查

---

## 七、边界

- **不替代人工签字**：凡用于监管申报、方案定稿、安全性判断的内容，本 Skill 只减轻复核负担，最终必须由有资质的人确认
- **不改文件**：默认只出问题清单。若用户要求直接改，改完必须做全文差异比对，确认"只动了预期位置"（AI 编辑引入的新错误与原有错误同样常见，详见方法论第四节）
- **扫描版 PDF**：只能判断"有没有 OCR"，OCR 文字准确性仍需人工抽查
- **规范时效**：CDE / FDA 格式要求会更新，规则集数值见 `references/`，各自标注了出处与核对日期，正式使用前建议核对官方最新版本
