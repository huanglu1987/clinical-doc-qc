# clinical-doc-qc · 临床试验资料核查 Skill

一个给 **Codex / Claude Code** 用的 Skill：全面核查临床试验申报资料的
**一致性、逻辑性、语言自然性、格式规范**，支持**单文件内部核查**与**多文件交叉核查**，
按 **CDE（中文）** 和 **FDA（英文）** 两套规则集分别判定格式合规性。

适用资料：临床试验方案、知情同意书（ICF）、研究者手册（IB）、临床研究报告（CSR）、
IND / NDA 各模块，以及任何需要前后一致的申报文件。

---

## 目录

- [它解决什么问题](#它解决什么问题)
- [核心设计：两层分离](#核心设计两层分离)
- [安装](#安装)
- [怎么用](#怎么用)
  - [用法一：让 AI 做全面核查（最常用）](#用法一让-ai-做全面核查最常用)
  - [用法二：只跑格式检查（几秒钟）](#用法二只跑格式检查几秒钟)
  - [用法三：多文件交叉核查](#用法三多文件交叉核查)
  - [用法四：双通道核查（正式提交必做）](#用法四双通道核查正式提交必做)
- [能查出什么](#能查出什么)
- [CDE / FDA 规则对照](#cde--fda-规则对照)
- [输出示例](#输出示例)
- [常见问题](#常见问题)
- [边界与免责](#边界与免责)

---

## 它解决什么问题

一次真实的核查复盘发现：

> 用"关键词命中/未命中矩阵"做完 21 项跨文档比对，**全部通过**，结论"可定稿"；
> 换一种查法后，在**同一批文件**里查出 **10 条高优先级矛盾**，结论变成"不可定稿"。
> 其中 3 条还是核查者自己前几轮亲手留下的。

**核查失效，绝大多数不是"没查"，而是"用错了查法"。**
字符串比对只能证明"同一个词写法一致"，但真正致命的是
**用不同的词表达了互斥的要求**——这类问题字符串比对完全无感。

本 Skill 把这套方法论固化下来，并补上 AI 单独做不到的格式层检查。

---

## 核心设计：两层分离

**这是本项目最重要的设计，也是不这么做就一定会出错的地方。**

> AI 读到的文档是纯文本，**不携带字体、字号、行距、页边距信息**。
> 让 AI"读一遍"判断排版是否合规，它只会编造结论。

所以核查被严格拆成两层：

| 层 | 查什么 | 由谁执行 |
|---|---|---|
| **格式层 + 结构层** | 字体、字号、行距、颜色、页边距、纸张；表图编号连续性、交叉引用悬空、缩写定义冲突 | `scripts/docqc.py` 解析文件 |
| **文本层** | 内容前后矛盾、逻辑自洽性、语言自然性 | AI 按 `references/methodology.md` |

工具只报告**事实**（"正文实际是宋体 12pt、左边距 2.1cm"），
规则集才做**判断**（"2.1cm < 2.5cm，违反 CDE 规定"）。
这样规则改了不用改代码，也不会出现 AI 猜字体的情况。

---

## 安装

### 前置要求

- Python 3.8+
- Codex CLI 或 Claude Code

### 一键安装

```bash
git clone https://github.com/huanglu1987/clinical-doc-qc.git
cd clinical-doc-qc
bash install.sh
```

`install.sh` 会做三件事：安装 Python 依赖 → 把 Skill 复制到 `~/.codex/skills/clinical-doc-qc/` → 跑一次自检。

### 手动安装

```bash
pip3 install python-docx pypdf pymupdf
mkdir -p ~/.codex/skills
cp -r clinical-doc-qc ~/.codex/skills/
```

Claude Code 用户把目标路径换成 `~/.claude/skills/`。

### 验证安装

```bash
python3 ~/.codex/skills/clinical-doc-qc/scripts/docqc.py --help
```

能看到帮助信息即安装成功。

---

## 怎么用

### 用法一：让 AI 做全面核查（最常用）

装好之后，在 Codex 里**直接说人话**，不用记命令：

```
用 clinical-doc-qc 按 CDE 规则审核这套资料：
~/项目/ZYG24004/方案_V2.0.docx
~/项目/ZYG24004/ICF_受试者版.docx
~/项目/ZYG24004/IB_V3.docx
```

AI 会自动：

1. 运行 `docqc.py` 拿到格式层与结构层的**事实**
2. 按方法论做文本层核查——**先建概念清单**（动词与规则），而不是直接做字符串比对
3. 追数值依赖链、穷举封闭性措辞、代入场景走条件条款
4. 按 `language-check.md` 判据核查语言自然性
5. 输出一张带 **触发规则 / 严重度 / 两处原文摘录** 的问题清单

只审一份方案（单文件内部一致性）也一样：

```
用 clinical-doc-qc 审核这份方案，重点看前后一致性：~/项目/方案_V2.0.docx
```

### 用法二：只跑格式检查（几秒钟）

不需要 AI 参与，命令行直接跑：

```bash
# 中文资料，递交 CDE
python3 ~/.codex/skills/clinical-doc-qc/scripts/docqc.py check 方案.docx --profile cde

# 英文资料，递交 FDA
python3 ~/.codex/skills/clinical-doc-qc/scripts/docqc.py check protocol.docx --profile fda

# 输出 JSON，便于接入其他流程
python3 .../docqc.py check 方案.docx --profile cde --json
```

只想看事实、不要判定：

```bash
python3 .../docqc.py extract 方案.docx
```

### 用法三：多文件交叉核查

多个文件一起传给 `check`，会自动附带跨文件一致性检查：

```bash
python3 .../docqc.py check 方案.docx ICF.docx IB.docx --profile cde
```

只看文件之间的差异：

```bash
python3 .../docqc.py compare 方案.docx ICF.docx IB.docx
```

会查出：跨文件字体/字号/页边距不统一、**同一缩写在不同文件里被定义成不同全称**
（多文档最高发的一致性问题）。

### 用法四：双通道核查（正式提交必做）

**实测数据：两份独立报告合并后 30 条发现，只有 1 条重合。**
因为不同执行实例的方法偏好不同，盲区也不同——
一方强于数值复算与格式对称性，另一方强于语义矛盾与递交完整性。

所以正式提交的材料，**不要只跑一轮**：

1. 打开 `templates/review-prompt.md`，把 `[方括号]` 换成本项目信息
2. 在**两个互不共享历史的新会话**里各粘一遍（不是同一个会话跑两次）
3. 人工合并两份结果，**逐条回原文验证**（实测对方也会有误报）

> 这一步 Skill 强制不了，必须人工执行。投入产出比是所有招数里最高的。

---

## 能查出什么

### 格式层（工具自动判定）

| 检查项 | CDE | FDA |
|---|---|---|
| 中文字体 | ✅ 宋体 | — |
| 西文字体 | ✅ Times New Roman | ✅ Times New Roman / Arial |
| 正文字号 | ✅ 小四（12pt） | ✅ 12pt |
| 表格字号 | ✅ ≥五号（10.5pt） | ✅ ≥10pt |
| 行距 | ✅ 1.5 倍 | — |
| 字体颜色 | ✅ 黑色 | — |
| 纸张 | ✅ A4 | — |
| 页边距 | ✅ 分纵/横向判定 | ✅ ≥1 英寸 |
| PDF 版本 / 加密 / JavaScript | — | ✅ |
| PDF 书签 / 字体嵌入 / 可搜索 | ✅（部分） | ✅ |
| 文件大小 ≤200MB | ✅ | — |

### 结构层（工具自动判定，CDE / FDA 通用）

- ✅ **表 / 图 / 附录编号是否连续、有无重复**（如"表编号从 1 到 3，中间缺 2"）
- ✅ **交叉引用是否悬空**（正文写"详见表 5"，但全文根本没有表 5）
- ✅ **同一缩写被定义成不同全称**（AE 一处是"不良事件"、另一处是"不良反应"）
- ✅ **高频使用但从未定义的缩写**
- ✅ 标题层级跳级（H1 直接跳 H3）
- ✅ 同一样式内部字体/字号不统一
- ✅ 页边距在文档内不统一

### 文本层（AI 按方法论核查）

- 语义矛盾——**不同措辞表达出互斥要求**（危害最高，字符串比对完全无感）
- 数值依赖链断裂（改了剂量没重算累计暴露量）
- 封闭性措辞名不副实（自称"汇总了全部要求"，实际漏收）
- 条件性条款不可执行（"参照末次给药 24 小时采样"在提前停药场景下时间点落在过去）
- 中文欧化句式 / 英文 Chinglish、时态漂移、情态动词（must/should/may）误用

---

## CDE / FDA 规则对照

| 项目 | CDE | FDA |
|---|---|---|
| 纸张 | **A4**（21×29.7cm） | US Letter |
| 中文字体 | **宋体**（强制） | 不适用 |
| 西文字体 | **Times New Roman** | Times New Roman **或 Arial** |
| 正文字号 | 中文不小于小四（12pt） | 12pt |
| 表格字号 | 不小于五号（10.5pt） | ≥10pt（9pt 为下限） |
| 行距 | 1.5 倍 | 未强制 |
| 页边距 | 纵向：左2.5 / 上2.0 / 其他1.0 cm<br>横向：上2.5 / 右2.0 / 其他1.0 cm | 四边 ≥1 英寸<br>（硬下限：左0.75in、其他0.375in） |

**中美双报同一品种时两套都要跑。** 最容易出错的两点：
纸张（A4 vs Letter，换语言递交时不会自动改）；
字体（FDA 允许 Arial，CDE 只认 Times New Roman + 宋体）。

规则出处与核对日期见 [`references/cde-format-spec.md`](references/cde-format-spec.md)
与 [`references/fda-format-spec.md`](references/fda-format-spec.md)。

---

## 输出示例

```
$ python3 docqc.py check 方案_V2.0.docx --profile cde

══════════════════════════════════════════════════════════════════════
方案_V2.0.docx　[CDE 规则集]
══════════════════════════════════════════════════════════════════════
  [✗ 错误] 中文字体：正文中文主字体为「微软雅黑」，规范要求「宋体」
  [✗ 错误] 西文字体：西文主字体为「Calibri」，规范要求「Times New Roman」
  [✗ 错误] 正文字号：正文主字号 10.5pt（五号），低于规范下限 12.0pt（小四）
  [✗ 错误] 表格字号：表格最小字号 8.0pt，低于规范下限 10.5pt（五号）
  [✗ 错误] 行距：未检出 1.5 倍行距，实际为：1.0倍（7段）
  [✗ 错误] 字体颜色：检出非黑色文字 15 字：#FF0000；规范要求黑色
  [✗ 错误] 页边距：第1节（纵向）左边距 2.0cm，低于规定的 2.5cm
  [✗ 错误] 表编号不连续：表编号从 1 到 3，中间缺 [2]
  [✗ 错误] 表交叉引用悬空：正文引用了 表 ['5']，但全文没有找到对应的表标题
  [✗ 错误] 缩写定义冲突：缩写「AE」在文中对应了多个全称：不良反应；不良事件
  [⚠ 警告] 缩写未定义：ITT(4次) 多次出现但全文未见「全称（缩写）」式定义
  [⚠ 警告] 标题层级跳级：从 H1 直接跳到 H3：「1.1.1 深层小节」

合计：10 项错误、2 项警告。
```

---

## 常见问题

**Q：报错 "不支持旧版 .doc"**
先在 Word 里另存为 `.docx`。旧版二进制格式没有可解析的样式结构。

**Q：提示缺少 python-docx / pypdf / pymupdf**
```bash
pip3 install python-docx pypdf pymupdf
```

**Q：扫描版 PDF 能查吗？**
只能判断"有没有 OCR 文字层"。OCR 认错字查不出来，仍需人工抽查。

**Q：为什么合规的文档也报"警告"？**
警告是"合规但异常"，比如边距远大于规定值——通常是手工调格式留下的痕迹，
提示你确认是否有意为之，不一定要改。

**Q：CDE 规则里的数值可靠吗？**
出自《药品注册申报资料格式体例与整理规范》（CDE，2020-10-01 施行），
经两处公开转载全文相互印证。**但未直接读取 CDE 官网附件原件**，
正式申报前建议以官网现行有效版本复核一次。

**Q：部门有自己的模板，比 CDE 更严，怎么办？**
在 `scripts/docqc.py` 的 `CDE_SPEC` 旁按同样结构加一个 `DEPT_SPEC`，
注册到 `PROFILES` 字典即可，不需要改检查逻辑。

**Q：能直接帮我改文件吗？**
默认只出问题清单。若要求直接改，**改完必须做全文差异比对**确认"只动了预期位置"——
AI 编辑引入的新错误与原有错误同样常见（批量替换过度命中、删段留空壳、
把内容插进目录而不是正文）。详见 `references/methodology.md` 第四节。

---

## 边界与免责

- **不替代人工签字**。凡用于监管申报、方案定稿、安全性判断的内容，
  本工具只减轻复核负担，最终必须由有资质的人确认。
- **法规会更新**。CDE / FDA 格式要求随版本变化，规则集数值均标注了出处与核对日期，
  正式使用前请核对官方最新版本。
- **文本层结论由 AI 生成**，用于快速定位问题，不能替代对原文的核实。
- 本工具**不上传任何文件内容**，全部在本地解析。

---

## 项目结构

```
clinical-doc-qc/
├── SKILL.md                        Skill 主入口：工作流与强制约束
├── scripts/docqc.py                格式层 + 结构层核查工具
├── references/
│   ├── cde-format-spec.md          CDE 规格 + 出处
│   ├── fda-format-spec.md          FDA 规格 + 出处
│   ├── methodology.md              一致性核查方法论（九条规则 + 双通道）
│   └── language-check.md           中/英文语言自然性判据
├── templates/review-prompt.md      核查提示词模板（双通道用）
└── install.sh                      一键安装
```

## License

MIT
