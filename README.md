# clinical-doc-qc · 临床试验资料核查 Skill

一个给 **Codex / Claude Code** 用的 Skill：全面核查临床试验申报资料的
**一致性、逻辑性、语言自然性、格式规范**，支持**单文件内部核查**与**多文件交叉核查**，
按 **CDE（中文）** 和 **FDA（英文）** 两套规则集分别判定格式合规性。

适用资料：临床试验方案、知情同意书（ICF）、研究者手册（IB）、临床研究报告（CSR）、
IND / NDA 各模块，以及任何需要前后一致的申报文件。

---

## 目录

- [它解决什么问题](#它解决什么问题)
- [核心设计：两层分离](#核心设计两层分离)
- [安装](#安装)
- [怎么用](#怎么用)
  - [用法一：全面深度核查（最常用）](#用法一全面深度核查最常用)
  - [用法二：格式 + 文字校对](#用法二格式--文字校对比用法一轻定稿前过一遍)
  - [用法三：双通道核查（正式提交必做）](#用法三双通道核查正式提交必做)
  - [命令速查](#命令速查)
- [能查出什么](#能查出什么)
- [CDE / FDA 规则对照](#cde--fda-规则对照)
- [输出示例](#输出示例)
- [常见问题](#常见问题)
- [边界与免责](#边界与免责)

---

## 它解决什么问题

一次真实的核查复盘发现：

> 用"关键词命中/未命中矩阵"做完 21 项跨文档比对，**全部通过**，结论"可定稿"；
> 换一种查法后，在**同一批文件**里查出 **10 条高优先级矛盾**，结论变成"不可定稿"。
> 其中 3 条还是核查者自己前几轮亲手留下的。

**核查失效，绝大多数不是"没查"，而是"用错了查法"。**
字符串比对只能证明"同一个词写法一致"，但真正致命的是
**用不同的词表达了互斥的要求**——这类问题字符串比对完全无感。

本 Skill 把这套方法论固化下来，并补上 AI 单独做不到的格式层检查。

---

## 核心设计：两层分离

**这是本项目最重要的设计，也是不这么做就一定会出错的地方。**

> AI 读到的文档是纯文本，**不携带字体、字号、行距、页边距信息**。
> 让 AI"读一遍"判断排版是否合规，它只会编造结论。

所以核查被严格拆成两层：

| 层 | 查什么 | 由谁执行 |
|---|---|---|
| **格式层 + 结构层** | 字体、字号、行距、颜色、页边距、纸张；表图编号连续性、交叉引用悬空、缩写定义冲突 | `scripts/docqc.py` 解析文件 |
| **文本层** | 内容前后矛盾、逻辑自洽性、语言自然性 | AI 按 `references/methodology.md` |

工具只报告**事实**（"正文实际是宋体 12pt、左边距 2.1cm"），
规则集才做**判断**（"2.1cm < 2.5cm，违反 CDE 规定"）。
这样规则改了不用改代码，也不会出现 AI 猜字体的情况。

---

## 安装

### 前置要求

- Python 3.8+
- Codex CLI 或 Claude Code

### 一键安装

```bash
git clone https://github.com/huanglu1987/clinical-doc-qc.git
cd clinical-doc-qc
bash install.sh
```

`install.sh` 会做三件事：安装 Python 依赖 → 把 Skill 复制到 `~/.codex/skills/clinical-doc-qc/` → 跑一次自检。

### 手动安装

```bash
pip3 install python-docx pypdf pymupdf
mkdir -p ~/.codex/skills
cp -r clinical-doc-qc ~/.codex/skills/
```

Claude Code 用户把目标路径换成 `~/.claude/skills/`。

### 验证安装

```bash
python3 ~/.codex/skills/clinical-doc-qc/scripts/docqc.py --help
```

能看到帮助信息即安装成功。

---

## 怎么用

三种用法按**核查深度**区分，不按文件数量区分——
**每一种都既能查一份文件、也能一次查多份**：传一份就是单文件内部核查，
传多份会自动附带跨文件比对（字体字号是否统一、同一缩写在不同文件里有没有被定义成不同全称等）。

| 用法 | 什么时候用 | 深度 | 需要 AI |
|---|---|---|---|
| **一 · 全面深度核查** | 正式提交前、需要查逻辑与语义矛盾 | 最深：概念清单 + 数值依赖链 + 语义比对 + 格式 + 语言 | 是 |
| **二 · 格式 + 文字校对** | 定稿前快速过一遍 | 较浅：格式 + 错别字 + 用词 + 目录 + 编号引用 | 是 |
| **三 · 双通道核查** | 正式提交材料**必做** | 用法一跑两遍再合并 | 是（两个独立会话） |

只想要客观格式事实、不需要 AI 的，直接跑命令行即可（见用法二末尾）。

### 用法一：全面深度核查（最常用）

装好之后，在 Codex 对话框里用自然语言描述需求即可，不需要记命令行参数。
以下提示词可以直接复制修改使用。

> **四个示例都是完整版，可直接整段复制使用**，不需要互相拼接。
> 它们与 [`templates/review-prompt.md`](templates/review-prompt.md) 的基线保持同步；
> 该模板是维护主本，正式提交材料请用模板 + 用法三的双通道。

#### 示例 1 · 多份资料交叉核查（CDE / 中文）

```
你是一名独立的文档核查员，不参与本套资料的起草，只挑问题，不要为已有写法辩护。

【核查对象】
文件清单（唯一事实来源＝方案）：
~/项目/方案_V2.0.docx
~/项目/ICF_受试者版.docx
~/项目/IB_V3.docx
递交去向：CDE　　资料语种：中文

【通则】凡可实测的断言，必须实际执行后再下结论
能运行、能计算、能打开的就去做，不要凭推断。未实际执行过的判断一律标"待人工复核"。

【第零步】格式层先跑工具，禁止凭阅读判断字体、字号、行距、页边距
执行：python3 ~/.codex/skills/clinical-doc-qc/scripts/docqc.py check <上述文件> --profile cde
- 工具报的"残留修订痕迹/残留批注/域代码错误"请置顶单列，它们会被接收方直接看到
- 目录：核对目录条目与正文标题是否一一对应；.docx 的目录页码是域的缓存值、
  渲染时会重算，不要判定页码对错，只提示"更新域后导出 PDF 再核对"

【第一步】先建概念清单，不要直接开始比对
自行通读全文，枚举 8–15 个关键操作概念（动词与规则：怎么做、什么时候做、谁决定、
多长时间、什么情况例外），覆盖范围由文档内容决定。对每个概念把全文相关句子
全部摘出来，两两比对是否可能同时成立。
以下几项并入清单并优先处理，但这只是补充、不是核查范围的全部，
你自行枚举出的其他概念同样要查：
入排标准、剂量与给药方案、访视安排、安全性事件上报时限、退出与终止规则

【第二步】三类不一致分开查，重点在第②类
①表层：同一数值/名称在各处写法是否一致（日期格式、缩写、单位）
②语义（权重最高）：不同措辞是否表达出互斥的要求，字面一致不代表通过
③结构：编号、交叉引用、合计数、时态与状态标注
术语要扫缩写、拼写全称、同义表达三种形态——只扫缩写会漏掉全称；
同义表达（受试者/患者/病人）工具查不了，必须由你判断。

【第三步】跨文件核查（本次为多份文件，必做）
以方案为基准，按三种不同关系逐项核对，不要只查"字面是否一致"：
① 一致性（双向）：同一事项在各文件中的表述是否冲突
② 覆盖性（方案→ICF/IB）：方案中对受试者有影响的内容，派生文件是否都覆盖到了
   （方案写了某项风险或某项检查、ICF 却未告知＝缺陷）
③ 依据性（ICF/IB→方案）：派生文件的每条陈述是否都能在方案里找到依据
   （ICF 承诺了方案中并不存在的补偿或随访安排＝过度承诺）
必核项：研究目的与主要次要终点、入排标准（方案正式表述 vs ICF 面向受试者的描述）、
样本量（总数/各组/各中心）、给药方案（剂量/频次/途径/疗程）、
访视次数与各访视时间点及窗口、研究总时长、风险与不良反应（方案安全性 vs ICF 告知
vs IB 已知）、受试者权利（退出条件/撤回知情同意的后果/已采集数据与样本如何处理）、
补偿赔偿与保险、数据保密与样本留存及后续使用、申办方与研究机构及联系人信息、
各文件版本号与日期是否互相对应（ICF 引用的方案版本是否为当前版本）。
未列到但你识别出的同样要查。

【第四步】数值依赖链
凡涉及计算/推导的数字，画出依赖链逐环核对：
单位用量→频次→单次小计→累计总量→据此推导的阈值→引用该阈值的入排标准→ICF/摘要等对外文件
表格分项之和必须与所载合计逐个验算，并在报告中给出算式。

【第五步】高危措辞专项复核
- "全部/唯一/完整/汇总了所有"等封闭性措辞，逐条穷举验证是否真的完整
- 条件性/例外性条款，代入具体场景实际走一遍确认物理上可执行：
  提前退出、提前终止、中断后恢复、周末与节假日、极值人群（最低体重/最高年龄）、
  某中心不具备某条件
- 法定/合规措辞回查是否确实适用于本研究，不要假设模板照抄就对

【第六步】错别字、用词与语言
①错别字（必报）：同音字（的地得、须需、已己）、形近字、多字漏字、
  单位大小写（mL/mg/μg）
②用词错误（必报）：不良事件与不良反应、发生率与发病率、疗效与有效性、
  剂量与剂型与规格、入选与入组与筛选、失访与脱落与退出
③语言自然性：只报造成歧义的、明显机翻腔/语法错误的、与全文风格不一致的，
  不要输出"建议润色""可更简洁"这类无判据的意见
同一错误重复出现合并成一条并注明次数。

【强制约束】
- 判定为"缺失/未提及"的项，一律标"待人工复核"，不得直接写成结论
  （实测约三分之一的"未命中"是假阴性：折行、破折号变体、直弯引号、上标、域代码等）
- 每项结论必须三选一：一致/不一致/不适用——先判断该文件是否本应涉及这一项，
  不能把沉默当成一致
- 不要在同一轮里自我评价"整体没问题"，只输出发现清单
- 违反以上任何一条方法得出的发现，不予采信

【输出，分四部分】
一、覆盖范围声明：列出实际建立的完整概念清单，每项标注来源（自行枚举/我点名）
   与结论；任何未覆盖的范围必须显式写出，不得静默省略
二、问题清单表：序号|触发规则（概念清单/表层/语义/结构/跨文件/数值链/封闭措辞/
   条件场景/语言/编辑痕迹/格式层工具）|严重度（高/中/低，注明依据：科学有效性/
   受试者安全/法规合规）|位置|原文摘录A|冲突摘录B|算式（数值类必填：算式+计算结果+
   文档所载值+是否相符）|结论|说明
   语义矛盾必须给两处原文摘录；数值类必须给算式，不能只写"不一致"
三、假阴性复核记录（单列）：凡判为"缺失/未提及"的项，逐条列出判定依据、
   是否已回原文人工确认、确认结果
四、不适用清单（单列，与"一致"分开）：本文件本就不涉及的项及其理由
```

> **为什么第一步要写"不是核查范围的全部"。** 如果只写"重点核查以下五项"，
> AI 很容易把这五项当成范围上限，其余部分查得很浅。而人在列举时往往是凭印象举例、
> 不是穷举——**你能想到的风险点，恰恰是已经注意过、因而不太容易出错的部分**，
> 真正的问题常在没想到的地方。

#### 示例 2 · 单份方案的内部一致性（CDE / 中文）

```
你是一名独立的文档核查员，不参与本文件的起草，只挑问题，不要为已有写法辩护。

【核查对象】
文件：~/项目/方案_V2.0.docx
递交去向：CDE　　资料语种：中文
本次为单文件核查，重点是同一份文件内前后是否自洽。

【通则】凡可实测的断言，必须实际执行后再下结论
能运行、能计算、能打开的就去做，不要凭推断。未实际执行过的判断一律标"待人工复核"。

【第零步】格式层先跑工具，禁止凭阅读判断字体、字号、行距、页边距
执行：python3 ~/.codex/skills/clinical-doc-qc/scripts/docqc.py check <文件> --profile cde
- 工具报的"残留修订痕迹/残留批注/域代码错误"请置顶单列
- 目录：核对目录条目与正文标题是否一一对应；.docx 的目录页码是域的缓存值、
  渲染时会重算，不要判定页码对错，只提示"更新域后导出 PDF 再核对"

【第一步】先建概念清单，不要直接开始比对
自行通读全文，枚举 8–15 个关键操作概念（动词与规则：怎么做、什么时候做、谁决定、
多长时间、什么情况例外），覆盖范围由文档内容决定。对每个概念把全文相关句子
全部摘出来，两两比对是否可能同时成立。
以下几项并入清单并优先处理，但这只是补充、不是核查范围的全部，
你自行枚举出的其他概念同样要查：
访视窗口、剂量调整规则、退出与终止标准、方案偏离处理、知情同意撤回后的数据处理

【第二步】三类不一致分开查，重点在第②类
①表层：同一数值/名称在各处写法是否一致（日期格式、缩写、单位）
②语义（权重最高）：不同措辞是否表达出互斥的要求，字面一致不代表通过
③结构：编号、交叉引用、合计数、时态与状态标注
术语要扫缩写、拼写全称、同义表达三种形态——只扫缩写会漏掉全称；
同义表达（受试者/患者/病人）工具查不了，必须由你判断。

【第三步】单文件内部的跨位置核查（本次必做）
同一份文件里，同一事项往往在多处重复出现，重复处之间最容易打架。逐项核对：
- 同一规则在不同章节是否存在互斥表述（访视窗口、剂量调整、退出标准等）
- 同一数值在正文、表格、流程图、附录、摘要中是否完全一致
- 正文描述与研究流程图/访视表是否吻合（访视次数、时间点、各访视检查项）
- 正文与附录的对应关系：附录声称汇总的内容是否与正文定义一致
- 摘要/概要部分与正文详细章节是否一致

【第四步】数值依赖链
凡涉及计算/推导的数字，画出依赖链逐环核对：
单位用量→频次→单次小计→累计总量→据此推导的阈值→引用该阈值的入排标准→摘要
表格分项之和必须与所载合计逐个验算，并在报告中给出算式。

【第五步】高危措辞专项复核
- "全部/唯一/完整/汇总了所有"等封闭性措辞，逐条穷举验证是否真的完整
- 条件性/例外性条款，代入具体场景实际走一遍确认物理上可执行：
  提前退出、提前终止、中断后恢复、周末与节假日、极值人群（最低体重/最高年龄）、
  某中心不具备某条件
- 法定/合规措辞回查是否确实适用于本研究，不要假设模板照抄就对

【第六步】错别字、用词与语言
①错别字（必报）：同音字（的地得、须需、已己）、形近字、多字漏字、
  单位大小写（mL/mg/μg）
②用词错误（必报）：不良事件与不良反应、发生率与发病率、疗效与有效性、
  剂量与剂型与规格、入选与入组与筛选、失访与脱落与退出
③语言自然性：只报造成歧义的、明显机翻腔/语法错误的、与全文风格不一致的，
  不要输出"建议润色""可更简洁"这类无判据的意见
同一错误重复出现合并成一条并注明次数。

【强制约束】
- 判定为"缺失/未提及"的项，一律标"待人工复核"，不得直接写成结论
  （实测约三分之一的"未命中"是假阴性）
- 每项结论必须三选一：一致/不一致/不适用——先判断本文件是否本应涉及这一项，
  不能把沉默当成一致
- 不要在同一轮里自我评价"整体没问题"，只输出发现清单
- 违反以上任何一条方法得出的发现，不予采信

【输出，分四部分】
一、覆盖范围声明：列出实际建立的完整概念清单，每项标注来源（自行枚举/我点名）
   与结论；任何未覆盖的范围必须显式写出
二、问题清单表：序号|触发规则|严重度（高/中/低，注明依据：科学有效性/受试者安全/
   法规合规）|位置|原文摘录A|冲突摘录B|算式（数值类必填）|结论|说明
   语义矛盾必须给两处原文摘录；数值类必须给算式
三、假阴性复核记录（单列）：凡判为"缺失/未提及"的项，逐条列出是否已回原文确认及结果
四、不适用清单（单列，与"一致"分开）
```

#### 示例 3 · 英文资料递交 FDA

```
你是一名独立的文档核查员，不参与本文件的起草，只挑问题，不要为已有写法辩护。

【核查对象】
文件：~/project/protocol_v3.docx
递交去向：FDA　　资料语种：英文

【通则】凡可实测的断言，必须实际执行后再下结论
能运行、能计算、能打开的就去做，不要凭推断。未实际执行过的判断一律标"待人工复核"。

【第零步】格式层先跑工具，禁止凭阅读判断字体、字号、页边距
执行：python3 ~/.codex/skills/clinical-doc-qc/scripts/docqc.py check <文件> --profile fda
- 工具报的"残留修订痕迹/残留批注/域代码错误"请置顶单列
- 目录：核对目录条目与正文标题是否一一对应；.docx 的目录页码是域的缓存值、
  渲染时会重算，不要判定页码对错，只提示"更新域后导出 PDF 再核对"
- 注意 FDA 与 CDE 规则不同：纸张为 US Letter 而非 A4，字体可用 Times New Roman
  或 Arial，页边距要求四边 ≥1 英寸

【第一步】先建概念清单，不要直接开始比对
自行通读全文，枚举 8–15 个关键操作概念（动词与规则），覆盖范围由文档内容决定。
对每个概念把全文相关句子全部摘出来，两两比对是否可能同时成立。
以下几项并入清单并优先处理，但这只是补充、不是核查范围的全部：
eligibility criteria、dosing regimen、visit schedule and windows、
AE/SAE reporting timelines、withdrawal and study termination rules

【第二步】三类不一致分开查，重点在第②类
①表层　②语义（权重最高）：不同措辞是否表达出互斥的要求　③结构
术语要扫缩写、拼写全称、同义表达三种形态——只扫缩写会漏掉全称。

【第三步】跨文件核查（若同时提交 protocol / ICF / IB，必做）
以 protocol 为基准，按一致性（双向）、覆盖性（protocol→ICF/IB）、
依据性（ICF/IB→protocol）三种关系逐项核对：
objectives and endpoints、eligibility criteria、sample size、dosing、
visit schedule、risks and adverse reactions、subject rights（withdrawal、
consequences of consent withdrawal、data and sample handling）、
compensation and insurance、confidentiality and sample retention、
sponsor/site/contact information、各文件版本号与日期是否互相对应。

【第四步】数值依赖链
画出依赖链逐环核对：unit dose→frequency→per-dose subtotal→cumulative total→
derived threshold→eligibility criteria referencing it→ICF/summary
表格分项之和必须与所载合计逐个验算，并在报告中给出算式。

【第五步】高危措辞专项复核
- "all / only / complete / comprehensive / exclusively" 等封闭性措辞，
  逐条穷举验证是否真的完整
- 条件性/例外性条款代入具体场景走一遍确认可执行：early withdrawal、
  early termination、resumption after interruption、weekends and holidays、
  extreme populations、site without a given capability
- 法定措辞回查是否确实适用于本研究：例如 "as required by law" 只适用于
  applicable clinical trial 的强制告知语，1 期研究套用即为错误

【第六步】拼写、用词与语言（英文判据）
①拼写与大小写（必报）：拼写错误；p-value / mL / μg / kg 的写法是否全篇统一；
  通用名小写、商品名首字母大写是否一致
②用词错误（必报）：affect 与 effect、dose 与 dosage、compliance 与 adherence、
  efficacy 与 effectiveness、incidence 与 prevalence、
  adverse event 与 adverse reaction
③时态（必查）：Protocol 应统一为前瞻性时态（will / is to be），不得混入过去时；
  CSR 应统一为过去时，不得混入 will。同一份文件里的时态漂移最常见于
  从方案复制到 CSR 的段落
④情态动词（必查，影响合规）：must / shall（强制）、should（推荐）、may（允许）
  使用是否准确。中文"应"翻译过来时最容易把强制写成推荐，或把推荐写成强制
⑤美式与英式拼写是否全篇统一（randomization / randomisation）
同一错误重复出现合并成一条并注明次数。

【强制约束】
- 判定为"缺失/未提及"的项，一律标"待人工复核"，不得直接写成结论
- 每项结论必须三选一：一致/不一致/不适用，不能把沉默当成一致
- 不要在同一轮里自我评价"整体没问题"，只输出发现清单
- 违反以上任何一条方法得出的发现，不予采信

【输出，分四部分】
一、覆盖范围声明（完整概念清单+来源+结论，未覆盖范围显式写出）
二、问题清单表：序号|触发规则|严重度（+依据）|位置|原文摘录A|冲突摘录B|
   算式（数值类必填）|结论|说明
三、假阴性复核记录（单列）
四、不适用清单（单列，与"一致"分开）
```

#### 示例 4 · 改完之后的复核

```
你是一名独立的文档核查员，不参与本文件的起草与修改，只挑问题，不要为已有写法辩护。

【核查对象】
文件：~/项目/方案_V2.1.docx（上一版为 V2.0）
上一轮问题清单：见下方粘贴 / 见附件
递交去向：CDE　　资料语种：中文

【通则】凡可实测的断言，必须实际执行后再下结论
能运行、能计算、能打开的就去做，不要凭推断。未实际执行过的判断一律标"待人工复核"。

【第零步】格式层先跑工具
执行：python3 ~/.codex/skills/clinical-doc-qc/scripts/docqc.py check <文件> --profile cde
- 工具报的"残留修订痕迹/残留批注/域代码错误"请置顶单列
  （改稿后尤其容易残留未接受的修订与讨论批注）
- 目录：核对目录条目与正文标题是否一一对应；.docx 页码为域缓存值，不判对错

【第一步】逐条对照上一轮清单
对上一轮的每一条，判定为：已改对 / 未改 / 改了但不彻底 / 改错了。
不要只看是否动过——要看改后的表述是否真的解决了原问题，
以及是否在解决 A 的同时制造了 B。

【第二步】专查修改本身引入的新问题
改文件时引入的新错误，与原有错误同样常见。逐项确认：
① 交叉引用是否被批量替换改坏——逐处确认指向对象，
   全局替换某个引用会连带改坏另一处本来正确的同名引用
② 有没有删段后留下的空段落、空表行、悬空标题、孤儿脚注、失效引用、目录脱节
③ 新增内容是否插到了正确位置——目录条目与正文标题同名时，
   应取最后一个匹配（正文那处），常见错误是插进了目录
④ 以标题段为模板克隆出的正文，是否连格式一起继承成了标题样式
⑤ 改了某个数值后，依赖它的下游数值是否全部重算（按依赖链逐环追）
⑥ 修改处有没有留下悬空的标点（删词后多出的逗号、顿号、括号）
⑦ 修改处的字体/字号/行距是否与全文一致

【第三步】重新做一遍完整核查，不要因为"只改了几处"就跳过
自行通读全文枚举 8–15 个关键操作概念，对每个概念把全文相关句子全部摘出来
两两比对；三类不一致分开查（表层/语义/结构），重点查语义类互斥要求；
术语扫缩写、全称、同义表达三种形态；
数值画依赖链逐环核对并给算式，表格分项之和与合计逐个验算；
封闭性措辞逐条穷举验证；条件性条款代入场景走一遍（提前退出、提前终止、
中断后恢复、周末节假日、极值人群、某中心不具备某条件）；
错别字与用词错误（不良事件与不良反应、发生率与发病率、剂量剂型规格、
入选入组筛选、失访脱落退出；单位大小写 mL/mg/μg）。

【第四步】若本文件属于一套多文件资料，还要做跨文件核查
改了方案却没同步改 ICF/IB，是改稿阶段最高发的问题。
以方案为基准，按一致性（双向）、覆盖性（方案→ICF/IB）、
依据性（ICF/IB→方案）三种关系，重点核对本次改动涉及的事项在其余文件中
是否已同步更新，以及各文件的版本号与日期是否仍然互相对应
（ICF 引用的方案版本是否还是当前版本）。

【强制约束】
- 判定为"缺失/未提及"的项，一律标"待人工复核"，不得直接写成结论
- 每项结论必须三选一：一致/不一致/不适用，不能把沉默当成一致
- 不要在同一轮里自我评价"整体没问题"，也不要因为"这版已经改过一轮"就默认更可靠
- 违反以上任何一条方法得出的发现，不予采信

【输出，分五部分】
一、上一轮清单的逐条处置结果（已改对/未改/改了但不彻底/改错了，各附证据）
二、覆盖范围声明（完整概念清单+来源+结论，未覆盖范围显式写出）
三、问题清单表：序号|触发规则|严重度（+依据）|位置|原文摘录A|冲突摘录B|
   算式（数值类必填）|结论|说明；并标明该问题是"原有遗留"还是"本次修改新引入"
四、假阴性复核记录（单列）
五、不适用清单（单列，与"一致"分开）
```


### 用法二：格式 + 文字校对（比用法一轻，定稿前过一遍）

用法一是**深度核查**（建概念清单、追数值链、查逻辑矛盾），耗时较长。
如果只想在定稿前快速过一遍**格式与文字**，用这个模式。

它同样需要 AI 参与——因为错别字、用词错误、目录内容是否对得上，工具判不了。

#### 示例提示词

```
用 clinical-doc-qc 对这份资料做「格式 + 文字校对」，递交去向 CDE：
~/项目/方案_V2.0.docx

这次只做格式与文字校对，不做深度逻辑核查。

通则：凡可实测的断言必须实际执行后再下结论——能运行、能计算、能打开的就去做，
不要凭推断。未实际执行过的判断一律标为"待确认"，不要写成结论。

覆盖以下几类：
1. 格式合规：先实际运行 docqc.py，不要凭阅读判断字体字号。
   把工具报的错误/警告整理成可执行的整改说明。
   工具会一并查出修订痕迹与批注残留、域代码错误、页眉页脚不一致、
   日期格式混用、半角标点、括号不配对、空段落、文档属性等。
   其中"残留修订痕迹/残留批注/域代码错误"请单独置顶列出——
   它们会被接收方直接看到，且递交后无法追回。
2. 错别字：同音字（的地得、须需、已己）、形近字、多字漏字、
   单位大小写（mL/mg/μg）。
3. 用词错误：容易混用且含义不同的词——
   不良事件与不良反应、发生率与发病率、疗效与有效性、
   剂量与剂型与规格、入选与入组与筛选、失访与脱落与退出。
4. 目录与正文：目录条目和正文标题是否对得上、有无遗漏或多余条目。
5. 其他：编号是否连续、交叉引用是否指向存在的对象、
   缩写首次出现是否定义、图表标题措辞风格是否统一。

要求：
- 同一个错误重复出现时合并成一条并注明次数，不要逐处刷屏。
- 每条给出所在位置与原文摘录，便于我直接定位修改。
- 不确定是不是错的（例如疑似专有名词、疑似有意为之的格式），
  单独列为"待确认"，不要直接判成错误。
- 报告开头写明"本次为格式+文字校对，未做深度逻辑与一致性核查"，
  让我清楚哪些没查。
```

英文资料把最后的去向换成 FDA，第 3 条换成：
`拼写错误、大小写一致性（p-value / mL / μg）、affect与effect、dose与dosage、efficacy与effectiveness`

#### 只要工具部分（不用 AI，几秒钟出结果）

```bash
# 中文资料，递交 CDE
python3 ~/.codex/skills/clinical-doc-qc/scripts/docqc.py check 方案.docx --profile cde

# 英文资料，递交 FDA
python3 ~/.codex/skills/clinical-doc-qc/scripts/docqc.py check protocol.docx --profile fda

# 输出 JSON，便于接入其他流程
python3 .../docqc.py check 方案.docx --profile cde --json
```

只想看事实、不要判定：

```bash
python3 .../docqc.py extract 方案.docx
```

#### ⚠ 关于目录页码：.docx 查不了，必须用 PDF

这是个容易踩的坑：**.docx 里目录的页码是「域」的缓存值**，
Word 打开或转 PDF 时会重新计算。在未渲染状态下读到的数字可能早已过期，
**据此判定"页码错误"会得出与事实相反的结论。**

所以工具的行为是：

| 格式 | 目录标题比对 | 目录页码比对 |
|---|---|---|
| `.docx` | ✅ 查（目录有而正文无 / 正文有而目录无） | ❌ 不判，只提示转 PDF 后复核 |
| `.pdf` | ✅ 查 | ✅ **真正比对**（页码已渲染，是实的） |

正确做法：**在 Word 里右键目录 → 更新域 → 更新整个目录 → 导出 PDF → 对 PDF 再跑一次。**

PDF 侧的页码比对还做了防误报处理：正文页码与 PDF 物理页序常有固定偏移
（封面、扉页占位），工具按**众数偏移**判定，只报偏离整体的条目，
不会因为"整体差 1 页"就把每一条都报成错。

### 用法三：双通道核查（正式提交必做）

**先解释这一步为什么必要。**

一次真实实验：让两个互不知情的执行者，用**同一份提示词**核查**同一批文件**，各出一份报告。
两份报告合并去重后共 **30 条**发现，其中**两边都查出来的只有 1 条**，
剩下 29 条都是只有一方发现、另一方完全漏掉的。

> 换句话说：**任何单独跑一轮的核查，都漏掉了另一轮能找到的绝大部分问题。**
> 不是说那 29 条有问题，而是说"只跑一轮"这件事本身不可靠。

原因是两边的方法偏好不同、盲区也就不同——
一方强于数值复算与格式对称性，另一方强于语义矛盾与递交完整性。

所以正式提交的材料，**不要只跑一轮**：

1. 打开 [`templates/review-prompt.md`](templates/review-prompt.md)，
   把【基线提示词】里的 `[方括号]` 换成本项目信息
2. 在**两个互不共享历史的新会话**里各粘一遍（**不是同一个会话里跑两次**——
   同一会话第二次会受第一次结论影响，等于没有第二个独立视角）
3. **给两轮各追加一个不同的镜头**（见下）
4. 人工合并两份结果，**逐条回原文验证**再采信
   （实测两份报告里也各有误报：一条把上标误读成章节号，一条没实测就下了结论）

#### 让两轮互补最大化：刻意分工

实测中两轮的强项自然分化了——一方强于数值复算与格式对称性，
另一方强于语义矛盾与递交完整性。与其指望这种分化偶然发生，不如**刻意分配**：

| | 追加镜头 | 侧重 |
|---|---|---|
| **通道 A** | 数值与结构 | 逐个复算、表格验算、编号引用对称性、跨文件数值一致、时间轴自洽 |
| **通道 B** | 语义与执行 | 互斥要求推演、条件场景可执行性、封闭措辞穷举、术语三形态、递交完整性 |

**两轮都要跑完整基线**，镜头是叠加在基线之上的——
这样既保留独立复核的价值（同一处被两边各查一次），
又让两边的额外投入落在不同地方。镜头段的完整文本见模板文件。

> 这一步 Skill 强制不了，必须人工执行。但它是所有招数里投入产出比最高的一个：
> 多花一轮的时间，换回来的是差不多翻倍的问题检出率。

> **合并完还要回头看一眼覆盖范围声明**：如果两份报告枚举出的概念高度雷同，
> 说明两轮可能被同一种思路带着走，此时补第三轮应该**换一个切入角度**
> （例如"假设我是核查员来现场检查，会先查什么"），而不是把同样的提示词再跑一遍。

### 命令速查

工具本身只有三个子命令，都支持一次传多个文件：

| 命令 | 作用 |
|---|---|
| `check <文件...> --profile cde\|fda` | **最常用**。按规则集判定；传多份时自动附带跨文件比对 |
| `compare <文件...>` | 只输出跨文件差异，不输出每份文件各自的问题。<br>适合"格式已经各自查过了，只想看几份之间统不统一"的场景 |
| `extract <文件...>` | 只输出格式事实（字体、字号、页边距、目录条目…），不做任何判定。<br>加 `--json` 可接入其他流程 |

```bash
# 一次核查三份，含跨文件比对
python3 ~/.codex/skills/clinical-doc-qc/scripts/docqc.py check 方案.docx ICF.docx IB.docx --profile cde

# 只看这三份之间统不统一
python3 ~/.codex/skills/clinical-doc-qc/scripts/docqc.py compare 方案.docx ICF.docx IB.docx
```

跨文件能查出：字体/字号/页边距不统一、
**同一缩写在不同文件里被定义成不同全称**（多文档最高发的一致性问题）。

---

## 能查出什么

### 格式层（工具自动判定）

| 检查项 | CDE | FDA |
|---|---|---|
| 中文字体 | ✅ 宋体 | — |
| 西文字体 | ✅ Times New Roman | ✅ Times New Roman / Arial |
| 正文字号 | ✅ 小四（12pt） | ✅ 12pt |
| 表格字号 | ✅ ≥五号（10.5pt） | ✅ ≥10pt |
| 行距 | ✅ 1.5 倍 | — |
| 字体颜色 | ✅ 黑色 | — |
| 纸张 | ✅ A4 | — |
| 页边距 | ✅ 分纵/横向判定 | ✅ ≥1 英寸 |
| PDF 版本 / 加密 / JavaScript | — | ✅ |
| PDF 书签 / 字体嵌入 / 可搜索 | ✅（部分） | ✅ |
| 文件大小 ≤200MB | ✅ | — |

### 结构层（工具自动判定，CDE / FDA 通用）

- ✅ **表 / 图 / 附录编号是否连续、有无重复**（如"表编号从 1 到 3，中间缺 2"）
- ✅ **交叉引用是否悬空**（正文写"详见表 5"，但全文根本没有表 5）
- ✅ **同一缩写被定义成不同全称**（AE 一处是"不良事件"、另一处是"不良反应"）
- ✅ **高频使用但从未定义的缩写**
- ✅ 标题层级跳级（H1 直接跳 H3）
- ✅ 同一样式内部字体/字号不统一
- ✅ 页边距在文档内不统一
- ✅ **目录条目与正文标题是否对得上**（目录有而正文无 / 正文有而目录无）
- ✅ **目录页码是否正确**（仅 PDF，见下方说明）

### 定稿前校对项（工具自动判定，`.docx`）

这一组的共同点是**会被接收方直接看到、且递交后无法追回**，
所以优先级高于其余格式问题：

| 检查项 | 级别 | 说明 |
|---|---|---|
| **残留修订痕迹** | ✗ 错误 | 未接受的插入/删除/移动/格式修订，接收方会看到全部修改历史 |
| **残留批注** | ✗ 错误 | 内部讨论的批注忘了删 |
| **域代码错误** | ✗ 错误 | "错误！未找到引用源"这类文本会直接印在纸面上 |
| 页眉页脚不一致 | ⚠ 警告 | 各节页眉不同——改版本号/方案编号时常漏改其中一处 |
| 日期格式混用 | ⚠ 警告 | `2026-01-15` 与 `2026年3月2日` 混用 |
| 中文句中半角标点 | ⚠ 警告 | `入组后,需完成评估;` 应为全角 |
| 括号不配对 | ⚠ 警告 | `详见附录（A。` |
| 连续空段落 | ⚠ 警告 | 删段后留下的空壳，带列表样式会渲染成空项目符号 |
| 表格空单元格 | ⚠ 警告 | 申报资料中空值一般应写"NA"而非留空 |
| 文档属性 | ◻ 待人工 | 作者/最后修改人可能带出上一版信息，递交前确认是否清除 |

### 文本层（AI 按方法论核查）

- 语义矛盾——**不同措辞表达出互斥要求**（危害最高，字符串比对完全无感）
- 数值依赖链断裂（改了剂量没重算累计暴露量）
- 封闭性措辞名不副实（自称"汇总了全部要求"，实际漏收）
- 条件性条款不可执行（"参照末次给药 24 小时采样"在提前停药场景下时间点落在过去）
- 中文欧化句式 / 英文 Chinglish、时态漂移、情态动词（must/should/may）误用

---

## CDE / FDA 规则对照

| 项目 | CDE | FDA |
|---|---|---|
| 纸张 | **A4**（21×29.7cm） | US Letter |
| 中文字体 | **宋体**（强制） | 不适用 |
| 西文字体 | **Times New Roman** | Times New Roman **或 Arial** |
| 正文字号 | 中文不小于小四（12pt） | 12pt |
| 表格字号 | 不小于五号（10.5pt） | ≥10pt（9pt 为下限） |
| 行距 | 1.5 倍 | 未强制 |
| 页边距 | 纵向：左2.5 / 上2.0 / 其他1.0 cm<br>横向：上2.5 / 右2.0 / 其他1.0 cm | 四边 ≥1 英寸<br>（硬下限：左0.75in、其他0.375in） |

**中美双报同一品种时两套都要跑。** 最容易出错的两点：
纸张（A4 vs Letter，换语言递交时不会自动改）；
字体（FDA 允许 Arial，CDE 只认 Times New Roman + 宋体）。

规则出处与核对日期见 [`references/cde-format-spec.md`](references/cde-format-spec.md)
与 [`references/fda-format-spec.md`](references/fda-format-spec.md)。

---

## 输出示例

```
$ python3 docqc.py check 方案_V2.0.docx --profile cde

══════════════════════════════════════════════════════════════════════
方案_V2.0.docx　[CDE 规则集]
══════════════════════════════════════════════════════════════════════
  [✗ 错误] 中文字体：正文中文主字体为「微软雅黑」，规范要求「宋体」
  [✗ 错误] 西文字体：西文主字体为「Calibri」，规范要求「Times New Roman」
  [✗ 错误] 正文字号：正文主字号 10.5pt（五号），低于规范下限 12.0pt（小四）
  [✗ 错误] 表格字号：表格最小字号 8.0pt，低于规范下限 10.5pt（五号）
  [✗ 错误] 行距：未检出 1.5 倍行距，实际为：1.0倍（7段）
  [✗ 错误] 字体颜色：检出非黑色文字 15 字：#FF0000；规范要求黑色
  [✗ 错误] 页边距：第1节（纵向）左边距 2.0cm，低于规定的 2.5cm
  [✗ 错误] 表编号不连续：表编号从 1 到 3，中间缺 [2]
  [✗ 错误] 表交叉引用悬空：正文引用了 表 ['5']，但全文没有找到对应的表标题
  [✗ 错误] 缩写定义冲突：缩写「AE」在文中对应了多个全称：不良反应；不良事件
  [⚠ 警告] 缩写未定义：ITT(4次) 多次出现但全文未见「全称（缩写）」式定义
  [⚠ 警告] 标题层级跳级：从 H1 直接跳到 H3：「1.1.1 深层小节」

合计：10 项错误、2 项警告。
```

---

## 常见问题

**Q：报错 "不支持旧版 .doc"**
先在 Word 里另存为 `.docx`。旧版二进制格式没有可解析的样式结构。

**Q：提示缺少 python-docx / pypdf / pymupdf**
```bash
pip3 install python-docx pypdf pymupdf
```

**Q：扫描版 PDF 能查吗？**
只能判断"有没有 OCR 文字层"。OCR 认错字查不出来，仍需人工抽查。

**Q：为什么合规的文档也报"警告"？**
警告是"合规但异常"，比如边距远大于规定值——通常是手工调格式留下的痕迹，
提示你确认是否有意为之，不一定要改。

**Q：CDE 规则里的数值可靠吗？**
出自《药品注册申报资料格式体例与整理规范》（CDE，2020-10-01 施行），
经两处公开转载全文相互印证。**但未直接读取 CDE 官网附件原件**，
正式申报前建议以官网现行有效版本复核一次。

**Q：部门有自己的模板，比 CDE 更严，怎么办？**
在 `scripts/docqc.py` 的 `CDE_SPEC` 旁按同样结构加一个 `DEPT_SPEC`，
注册到 `PROFILES` 字典即可，不需要改检查逻辑。

**Q：能直接帮我改文件吗？**
默认只出问题清单。若要求直接改，**改完必须做全文差异比对**确认"只动了预期位置"——
AI 编辑引入的新错误与原有错误同样常见（批量替换过度命中、删段留空壳、
把内容插进目录而不是正文）。详见 `references/methodology.md` 第四节。

---

## 边界与免责

- **不替代人工签字**。凡用于监管申报、方案定稿、安全性判断的内容，
  本工具只减轻复核负担，最终必须由有资质的人确认。
- **法规会更新**。CDE / FDA 格式要求随版本变化，规则集数值均标注了出处与核对日期，
  正式使用前请核对官方最新版本。
- **文本层结论由 AI 生成**，用于快速定位问题，不能替代对原文的核实。
- 本工具**不上传任何文件内容**，全部在本地解析。

---

## 项目结构

```
clinical-doc-qc/
├── SKILL.md                        Skill 主入口：工作流与强制约束
├── scripts/docqc.py                格式层 + 结构层核查工具
├── references/
│   ├── cde-format-spec.md          CDE 规格 + 出处
│   ├── fda-format-spec.md          FDA 规格 + 出处
│   ├── methodology.md              一致性核查方法论（九条规则 + 双通道）
│   └── language-check.md           中/英文语言自然性判据
├── templates/review-prompt.md      核查提示词模板（双通道用）
└── install.sh                      一键安装
```

## License

MIT
